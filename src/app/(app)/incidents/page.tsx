import { IncidentCommandView } from "@/components/incident-command-view";

export const metadata = { title: "Incidents — Pulse" };

export default function IncidentsPage() {
  return <IncidentCommandView />;
}
